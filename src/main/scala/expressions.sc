/**
 * Expressions
 */
val a = 100
val b = 10
val sum = a + b
val diff = a - b
val product = a * b
val quotient = a / b

val s = "HELLO"
val lower = s.toLowerCase()

/**
 * Statements
 */
// Statement
s.toLowerCase()

// Statement
println(s)
println(lower)

// Statement
println(sum, diff, product, quotient)
